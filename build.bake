import file from WebSiteSupport.bake
import file from Deploy.bake
import file from Lib.bake
import file from Test.bake
import System.IO

Global(
	project : "AdminInterface",
	deployTo : "\\\\acdcserv\\publish\\TestAdm",
	editor : "vim-nox",
	humanReadableName : "Адмнистративный интерфейс"
)

Task @default, [@build]

Task @build, [@buildWebSite]

Task @deploy, [@build, @deployWebSite, @SendDeployNotification]

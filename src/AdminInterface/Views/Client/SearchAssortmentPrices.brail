<% if prices.Count: %>
<select name="drugstore.BuyingMatrixPrice.Id" style="width: 100%">
	<% for i, price in enumerate(prices): %>
		<option value="${price.Id}">${price.Supplier.Name} - ${price.Name}</option>
	<% end %>
</select>
<% end %>
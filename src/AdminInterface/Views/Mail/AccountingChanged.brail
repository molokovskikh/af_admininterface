${admin.Name}
${System.DateTime.Now}
${admin.Host}
договор: ${payer.Id}
плательщик: ${payer.Name}
код клиента: ${service.Id}
название клиента: ${service.Name}
{account.Type}, ${account.Name}
было ${oldPayment.ToString("C")} стало ${newPayment.ToString("C")}
import Castle.ActiveRecord
import AdminInterface.Models.Billing
import AdminInterface.Models

task @CreateAccounting, [@InitActiveRecord]:
	using SessionScope():
		for user in ActiveRecordMediator[of User].FindAll():
			continue if user.Accounting
			user.Accounting = UserAccount(user)
			ActiveRecordMediator.Save(user)

import Castle.ActiveRecord
import AdminInterface.Models.Billing

task @UpdatePaymentSum, [@InitActiveRecord]:
	using SessionScope():
		for payer in Payer.FindAll():
			using SessionScope():
				payer = Payer.Find(payer.Id)
				payer.UpdatePaymentSum()
				payer.Save()
